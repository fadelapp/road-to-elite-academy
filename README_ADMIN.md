# Road to Elite — Back Office V4 corrigé

Cette version est compatible avec la table `profiles` actuelle, qui ne contient pas de colonne `is_active`.

## Mise en ligne
1. Remplacer les fichiers du dépôt GitHub par le contenu de ce dossier.
2. Commit puis Push avec GitHub Desktop.
3. Attendre le redéploiement automatique Vercel.
4. Ouvrir `/admin/` sur l'adresse du site.

Les droits SQL corrigés ont déjà été exécutés avec succès dans Supabase : il n'est pas nécessaire de les relancer.
